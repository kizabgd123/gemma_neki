# EDA and Feature Engineering Implementation Plan

## Overview
Develop a reproducible feature engineering pipeline for the S6E5 competition. This plan covers data ingestion, automated profiling, and the creation of predictive, leakage-safe features.

## Scope Definition (CRITICAL)
### In Scope
- Creation of `kaggle_agent.py` to handle data operations.
- Implementation of a feature engineering script (`src/feature_engineering.py`).
- Automated EDA and feature importance validation using a fast LGBM baseline.
- Exporting processed training and test artifacts.

### Out of Scope (DO NOT TOUCH)
- Final ensemble training (Ticket e5f6g7h8).
- Kaggle submission synchronization (Ticket i9j0k1l2).
- Modification of existing non-tabular agents.

## Current State Analysis
- Data is located in `/home/kizabgd/.gemini/tmp/memorijada/data/`.
- Existing agents in `agents/` follow a standard interface but lack tabular data specifics.
- `CLIAgent` can be used to run Python scripts but has a 60s timeout.

## Implementation Phases

### Phase 1: Infrastructure & Data Ingestion
- **Goal**: Establish the `KaggleAgent` and verify data access.
- **Steps**:
  1. [ ] Create `agents/kaggle_agent.py` extending `BaseAgent`.
  2. [ ] Implement `load_data` method in `KaggleAgent`.
  3. [ ] Verify `KaggleAgent` can read `train.csv` via a unit test.
- **Verification**: `python3 -m pytest tests/test_kaggle_agent.py`

### Phase 2: Feature Engineering Pipeline
- **Goal**: Implement leakage-safe features and categorical encoding.
- **Steps**:
  1. [ ] Create `src/feature_engineering.py` with `preprocess_data` function.
  2. [ ] Implement rolling window features (e.g., mean LapTime per Driver/Race).
  3. [ ] Implement categorical encoding (Frequency encoding for `Driver`, One-Hot for `Compound`).
  4. [ ] Create interaction features between `TyreLife` and `Compound`.
  5. [ ] Implement explicit leakage check (verify no correlation between features and current-lap pit indicators).
- **Verification**: Run `src/feature_engineering.py` and check output column counts.

### Phase 3: Validation & Artifact Export
- **Goal**: Validate feature quality and save processed datasets.
- **Steps**:
  1. [ ] Implement a fast LGBM baseline in `src/baseline.py`.
  2. [ ] Train using Stratified 5-Fold CV and report ROC AUC.
  3. [ ] Export `train_processed.parquet` and `test_processed.parquet`.
- **Verification**: `src/baseline.py` outputs AUC > 0.94.

### Phase 4: Integration with Orchestrator
- **Goal**: Wrap the pipeline into an agentic task.
- **Steps**:
  1. [ ] Update `execution/engine.py` or create a wrapper task for the FE pipeline.
  2. [ ] Ensure `KaggleAgent` can be called via the orchestrator.
- **Verification**: Run a sample DAG with `KaggleAgent`.
